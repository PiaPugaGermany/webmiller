<div class="container">


<h1>Mi perfil</h1>

<!-- <div class="container">
  <%# <p>Nombre de usuario: <%= @user.name%> </p> %>
  <p>
    <%# Foto de perfil: <%= image_tag @user.photo %> %>
  </p>

<%# <a href="<%= edit_user_registration_path(current_user)%>">Editar perfil </a> %>
</div> -->

<div class="card mb-3" style="max-width: 540px;">

  <div class="row no-gutters">
    <div class="col-md-4">
      <img src="<%= asset_path(@user.photo.url) %>" class="card-img" alt="...">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">Nombre de usuario: <%= @user.name%></h5>
        <p class="card-text">Hola <%= @user.name%>! En esta sección puedes editar tu foto de perfil, nombre de usuario y contraseña :)</p>
        <a href="<%= edit_user_registration_path(current_user)%>" class="btn btn-primary">Editar</a>
      </div>
    </div>
  </div>

</div>


</div>
